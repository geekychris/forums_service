package com.example.forum.model;

/**
 * Enum representing different types of content that can be attached to posts or comments.
 */
public enum ContentType {
    IMAGE,
    VIDEO,
    DOCUMENT,
    AUDIO
}

